// function_for_dates.cpp

#include <iostream>
#include <time.h>
#include "List.h"


using namespace std;

int main()
{
    cout << "\t\t\tDouble linked list";

    List lst;

    srand(time(0));

    int a, gen;//generetion random quanity of random numbers
    gen = rand() % 10 + 1;
    for (int i = 0; i < gen; i++) {
        a = rand() % 100;
        lst.push_back(a);
    }
    lst.Print();//output

    return 0;
}
